extends layout

block content
  h1= title
  p Welcome to this #{title}
  button.btn.btn-outline-success(type="button" onclick = "goFaster()") Press Me
  div#faster Increase the speed 

  script.
    let mph = 20;
    let change = 1;
    function goFaster(){
      mph += change;
      change+=1;
      document.getElementById('faster').textContent = `MPH ${mph} with change ${change}`;
    }

  a(href ="horse") horses page
